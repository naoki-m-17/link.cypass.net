# Firebase Admin（Firestore 接続用）
#
# 設定手順:
# 1. Firebase Console → プロジェクトの設定 → サービスアカウント
# 2. 「新しい秘密鍵の生成」で JSON をダウンロード
# 3. その JSON を 1 行の文字列にしてここに貼り付ける（改行を \n に置換するか、minify した文字列）
#
# 例（実際の値はダウンロードした JSON）:
# FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"your-project-id",...}

FIREBASE_SERVICE_ACCOUNT_KEY=

# 問い合わせフォーム（404 ページ）- Gmail API (OAuth 2.0)
#
# GMAIL_CLIENT_ID, GMAIL_CLIENT_SECRET: Google Cloud Console の OAuth 2.0 クライアント ID から取得
# GMAIL_REFRESH_TOKEN: scripts/get-gmail-refresh-token.mjs をpnpm gmail:token実行で取得
# GMAIL_USER: 送信元メールアドレス（OAuth 認可した Gmail アカウント）
# CONTACT_EMAIL_TO: 問い合わせの送信先メールアドレス

GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER=
CONTACT_EMAIL_TO=